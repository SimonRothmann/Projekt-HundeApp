# Dogity – Entwicklungs-TODO

Wird laufend von Claude gepflegt, damit jederzeit ein Überblick über den aktuellen Stand besteht. Spiegelt die interne Task-Liste.

## Erledigt

- [x] Git-Repo initialisieren
- [x] Doku-Dateien optimieren (TECH_STACK.md, DEPLOYMENT.md, README.md – Hosting ohne CI/CD-Pflicht)
- [x] Backend-Skeleton: ASP.NET Core 9 Clean Architecture
- [x] Core-Domainmodell + EF Core Migration
- [x] Frontend-Skeleton: Next.js + TypeScript + Tailwind + shadcn
- [x] Lokale Dev-Umgebung ohne Docker
- [x] Trainingstagebuch: Application-Service, API-Controller, Frontend-UI
- [x] Zielplanung: Domain, Migration, Application-Service + Plan-Generator, API-Controller, Frontend-UI
- [x] Community-Domain: Club/Group/Membership (Grundmodell)
- [x] Sportkatalog erweitert: Bewertungskriterien je Übung + drei Fährten-Prüfungsstufen (A/B/C)
- [x] GPS/Fährte-Modul: Domain + API + Frontend (Browser-Geolocation, Leaflet/OSM-Karte)
- [x] Trainer-Übersicht: Gruppen anlegen/verwalten, Mitglieder einladen, individuelle Pläne zuweisen
- [x] Admin-Übersicht (Plattform-Scope): Kennzahlen, Nutzerliste, Prüfungsordnungs-Metadaten pflegen
- [x] PWA/Offline-Unterstützung: Manifest, Service Worker, IndexedDB-Sync-Queue für Training/GPS
- [x] Vereinsverwaltung (Backend + Frontend): Admin legt Vereine an und weist Trainern die Vereins-Rolle zu (ClubTrainer); Trainer können Gruppen optional einem zugewiesenen Verein zuordnen
- [x] Übungs-Verwaltung (Backend + Frontend): Admin pflegt globale Übungen, Vereinstrainer ausschließlich vereinsspezifische Übungen ihres Vereins (Exercise.ClubId + Sichtbarkeitsfilter, per curl/End-to-End verifiziert)
- [x] Prüfungsordnung-Import (Backend + Frontend): Scan der lokalen PDF via `pdftotext`, Admin wählt einzelne Vorschläge gezielt aus und übernimmt sie in den Katalog
- [x] Trainer-Feedback zu Trainingseinheiten (Backend + Frontend): nur ein per TrainerAssignment zugewiesener Trainer kann Feedback zu einem Training eines betreuten Hundes geben, der Besitzer sieht es nur lesend (schließt die letzte offene Lücke aus ROADMAP.md Phase 2 "Trainer Plattform")
- [x] Fährtenaufzeichnung im Frontend prominenter gemacht: direkter "Fährte aufnehmen"-Button auf der Hundedetailseite (legt Training + GPS-Track in einem Schritt an, auch offline), statt vorher zwingend ein vollständiges bewertetes Training anlegen zu müssen
- [x] Testaccounts + Demo-Daten für alle Rollen (`DemoDataSeeder`, läuft automatisch in Development): `admin@dogity.test`, `trainer@dogity.test`, `mitglied1@dogity.test`, `mitglied2@dogity.test`, Passwort jeweils `Demo1234!`
- [x] Prüfungsordnung-Import gegen die echte PDF ausgeführt und validiert (Scan + Übernahme einzelner Kandidaten end-to-end per curl getestet)
- [x] IGP1-3 (FCI-Internationale Gebrauchshundeprüfung) als neue Sportarten im Katalog angelegt, mit Übungsnamen und Punktzahlen direkt aus der UTI-REG-IGP-de-2025-PDF (Ausnahme von der sonst geltenden "keine Originaltexte"-Regel, ausdrücklich genehmigt durch den Auftraggeber in seiner Funktion als VDH-Vorstand)
- [x] Lokale Entwicklungsumgebung auf macOS aufgesetzt (Postgres@17, poppler, .NET 9 zusätzlich zu .NET 10 via Homebrew, DB-Rolle/Datenbank/Secrets, Migrationen, Backend+Frontend erfolgreich gebaut/gestartet, End-to-End-Login per curl verifiziert). macOS-spezifische Eigenheiten (Postgres-Start-Bug, .NET-Mehrversionsbetrieb) in README.md "Besonderheiten auf macOS" dokumentiert, Start/Stop-Skripte unter `scripts/db-start.sh`/`scripts/db-stop.sh`. Fehlende `frontend/.env.local.example` (von README referenziert, aber nie committet) ergänzt.
- [x] Resilienz: Service Worker (`frontend/public/sw.js`) cachte alle Same-Origin-GET-Requests (außer Navigation) cache-first mit einem statischen, nie versionierten `CACHE_NAME` - nach einem Produktions-Deployment blieben dadurch alte Inhalte (RSC-Payloads, Icons, Manifest) potenziell unbegrenzt im Cache, sichtbar als "App zeigt veraltete Inhalte", behebbar bisher nur durch manuelles Löschen der Browserdaten. Strategie umgestellt: nur inhaltsbasiert gehashte `/_next/static/`-Assets bleiben cache-first (sicher, da Hash sich mit Inhalt ändert), alles andere ist jetzt network-first mit Cache nur als Offline-Fallback; `CACHE_NAME` auf `v2` angehoben, um einmalig alte Fehlversionen zu invalidieren.
- [x] Resilienz: Frontend behandelte abgelaufene/invalide JWTs nicht - nach Tokenablauf blieb die App in einem kaputten Zustand (jeder weitere Request scheiterte mit 401, ohne dass die UI reagierte). `frontend/src/lib/api.ts` erkennt jetzt 401-Antworten auf authentifizierte Requests (Token war gesetzt), räumt `localStorage` auf und leitet automatisch zu `/login` weiter; ein 401 ohne Token (z.B. falsches Passwort beim Login) bleibt unverändert eine normale Formularfehlermeldung.
- [x] Performance: kein Application-Service nutzte `AsNoTracking()` - auch reine Lese-Endpunkte (Ziele, Trainingstagebuch, Hunde, Sportkatalog, GPS-Tracks, Gruppen, Vereine) haben Entities unnötig im EF-Core-ChangeTracker gehalten. Gezielt auf Lesepfade ergänzt, die nicht im selben Methodenaufruf speichern (Schreibpfade unverändert, ggf. per `track`-Parameter getrennt, z.B. `GoalService.GetOwnedGoalAsync`, `TrainingService.GetOwnedSessionAsync`). Backend-Tests weiterhin grün.
- [x] Performance: Response-Compression (Brotli/Gzip) im Backend aktiviert (`Program.cs`) - reduziert Payload-Größe für Trainingslisten/GPS-Tracks auf mobilen Verbindungen spürbar, per curl verifiziert (`Content-Encoding: br`).
- [x] Bugfix `ExerciseDifficulty`-Typ-Mismatch: Backend serialisiert das Enum mangels `JsonStringEnumConverter` als Zahl (`"difficulty": 1`), Frontend hatte es aber als String-Union (`"Beginner"|"Intermediate"|"Advanced"`) typisiert - dadurch lieferte `difficultyLabel[ex.difficulty]` in vier Dateien (`dogs/[id]/page.tsx`, `sports/page.tsx`, `global-exercises-section.tsx`, `club-exercises-section.tsx`) zur Laufzeit `undefined` statt des deutschen Labels. `types.ts` auf `0 | 1 | 2` umgestellt (analog zu `DogGender`/`GoalStatus`), alle vier `difficultyLabel`-Records auf numerische Keys angepasst; in den beiden Select-Formularen zusätzlich `Number(value)`-Konvertierung ergänzt, da `Object.entries()` über die Record-Keys immer Strings liefert - sonst wäre beim Anlegen/Bearbeiten einer Übung ein String statt einer Zahl ans Backend gesendet worden. Typecheck, Lint, Build und End-to-End-Verifikation per curl erfolgreich.
- [x] Performance (mobil): GPS-Track-Payload für lange Fährten reduziert. Lange Aufzeichnungen (1 Punkt/Sekunde, siehe `GPS_MAX_POSITION_AGE_MS`) konnten tausende Punkte unkomprimiert über mobile Verbindungen ausliefern und client-seitig mit Leaflet rendern. Neuer `GpsTrackSimplifier` (Douglas-Peucker, 3m Toleranz - innerhalb der ohnehin üblichen Smartphone-GPS-Genauigkeit, ab 200 Punkten aktiv) wird beim Lesen (`GetByTrainingSessionAsync`) auf automatische Punkte angewendet; manuelle Marker und die in der DB gespeicherten Rohpunkte bleiben unverändert. 4 neue Unit-Tests (`GpsTrackSimplifierTests`), End-to-End per curl verifiziert: 500 Testpunkte auf einer Geraden mit einem bewussten Ausreißer wurden beim Abruf auf 5 Punkte reduziert (Anfang, Ende, Ausreißer korrekt erhalten).
- [x] Code-Verschlankung (Frontend): `haversineMeters`/`estimateLengthMeters` waren dreifach identisch dupliziert (`fahrte-recorder.tsx`, `gps-track-section.tsx`, `walk-run-recorder.tsx`) - jetzt zentral in `lib/geo.ts`. Der komplette GPS-Aufzeichnungs-Lifecycle (watchPosition/getCurrentPosition/clearWatch inkl. Cleanup beim Unmount) war ebenfalls in allen drei Komponenten nahezu identisch dupliziert - in den neuen Hook `lib/use-gps-recorder.ts` ausgelagert. Das vierfach duplizierte `difficultyLabel`-Record (`dogs/[id]/page.tsx`, `sports/page.tsx`, `global-exercises-section.tsx`, `club-exercises-section.tsx`) liegt jetzt zentral in `lib/constants.ts`. Typecheck, Lint, Build erfolgreich; Verhalten unverändert (manuell durchgesteuert: Aufnahme starten/markieren/stoppen je Komponente).
- [x] Bugfix Smartphone-Zugriff über LAN: Login (und jede andere Formular-Interaktion) funktionierte beim Aufruf über die LAN-IP des Entwicklungsrechners nicht - die Seite lud, aber Formulare lösten beim Absenden einen normalen Browser-Reload statt des JS-Submit-Handlers aus, Buttons reagierten nicht. Ursache: Next.js 15+ blockt seit einer Sicherheitsänderung standardmäßig Cross-Origin-Zugriffe auf Dev-Server-Assets (JS-Chunks/HMR) - das initiale HTML wird noch ausgeliefert, React hydratisiert aber nie, weil die Chunks mit 403 abgewiesen werden (sichtbar nur im Server-Log, nicht im Browser). Backend (CORS, Kestrel-Bind auf `0.0.0.0`) und macOS-Firewall waren bereits korrekt konfiguriert und nicht die Ursache. Fix: `allowedDevOrigins: ["192.168.1.*"]` in `frontend/next.config.ts` (Wildcard statt fester IP, damit eine andere DHCP-vergebene Adresse im selben Subnetz nicht erneut bricht). End-to-End auf echtem Smartphone verifiziert.
- [x] Bugfix GPS auf iPhone: Fährtenaufzeichnung schlug mit "keine Erlaubnis" fehl, **ohne dass der iOS-Berechtigungsdialog überhaupt erschien**. Ursache: Safari erlaubt `navigator.geolocation` nur in einem Secure Context (HTTPS) - außerhalb von `localhost` ohne Ausnahme, anders als z.B. Chrome auf Android/Desktop, das private LAN-IPs großzügiger behandelt. Die App lief bisher nur über `http://<LAN-IP>:3000`. Lösung: lokales HTTPS-Setup per `mkcert` (kostenlos, bleibt komplett im eigenen Netz, Alternative zu einem öffentlichen Tunnel-Dienst wäre möglich gewesen, aber bewusst nicht gewählt). Backend nutzt jetzt das `https`-Profil (Port 7297) mit dem mkcert-Zertifikat über `Kestrel:Certificates:Default` in `appsettings.Development.json`; Frontend hat ein neues Skript `npm run dev:https` (Next.js' `--experimental-https-key/-cert`). `lib/api.ts` wählt den Backend-Port jetzt anhand des Protokolls der aufrufenden Seite (`7297` bei HTTPS, `5080` bei HTTP) statt ihn aus der konfigurierten URL abzuleiten - sonst wäre der API-Call vom HTTPS-Frontend aus auf den falschen (reinen HTTP-)Port gegangen. Setup-Anleitung (inkl. einmaligem Zertifikat-Vertrauen auf dem iPhone per AirDrop) in README.md "GPS/Fährtenaufzeichnung vom iPhone testen". End-to-End auf echtem iPhone verifiziert: Standortdialog erscheint jetzt, Aufzeichnung läuft.
- [x] Fährtenaufzeichnung präzisiert (Winkel/Abbiegungen sichtbar, Live-Karte): Drei zusammenhängende Probleme behoben. (1) `GpsTrackSimplifier`-Toleranz war mit 3m zu großzügig - bei Schrittgeschwindigkeit (~1m zwischen Punkten) liegt eine scharfe Abbiegung oft nur 0,5-1m von der Geraden zwischen Vorgänger/Nachfolger entfernt und wurde dadurch weggeglättet; auf 1m Toleranz reduziert und die Schwelle, ab der überhaupt vereinfacht wird, von 200 auf 2000 Punkte angehoben (reguläre Trainingstracks bleiben jetzt komplett unverändert, die Vereinfachung greift nur noch als Sicherheitsnetz für pathologisch lange Aufzeichnungen). (2) `watchPosition()` hat keinen Parameter für die Abtastrate und wird von manchen Betriebssystemen (u.a. beobachtet auf iOS) zusätzlich aus Energiespargründen gedrosselt - `lib/use-gps-recorder.ts` nutzt jetzt einen eigenen Poll-Loop mit `getCurrentPosition()` alle 500ms (rekursives `setTimeout`, keine überlappenden Anfragen bei schlechtem Empfang), das erzwingt den gewünschten Takt, schneller als das tatsächliche Update-Intervall des GPS-Chips (üblich ca. 1 Hz) bringt das aber naturgemäß keine zusätzlichen echten Punkte. (3) Während der Aufnahme wurde bisher gar keine Karte angezeigt, nur ein Punktezähler - `TrackMap` hat jetzt einen `live`-Modus: Karte wird einmalig erzeugt und bei neuen Punkten nur noch inkrementell aktualisiert (kein Neuaufbau pro GPS-Tick, das hätte sonst geruckelt), Kartenmitte folgt per `panTo` dem aktuellen Standort bei festem Zoom-Level 18. Eingebunden in `fahrte-recorder.tsx`, `gps-track-section.tsx` und `walk-run-recorder.tsx`. Backend-Tests (Schwellwerte angepasst), Typecheck, Lint, Build erfolgreich.

- [x] Rebrand "CanisTrack" → "Dogity": komplette Umbenennung über das gesamte Repository - 6 Backend-Projekte (Verzeichnisse, `.csproj`, Namespaces/`using`, `.sln`), alle Dokumentations-Dateien, Frontend (Titel, Manifest, Service Worker, localStorage-Keys, API-Lib-Kommentare), lokale Postgres-Datenbank/Rolle (`canistrack_dev`/`canistrack` → `dogity_dev`/`dogity` per `ALTER DATABASE`/`ALTER ROLE`, Daten bleiben erhalten) und Demo-Account-E-Mail-Domain (`@dogity.test`). Backend-Build+Tests sowie Frontend-Typecheck/Lint/Build erfolgreich, Login mit umbenanntem Demo-Account per curl verifiziert.
- [x] Farbschema auf Apple-orientierte Palette umgestellt (siehe DESIGN_SYSTEM.md "Farben"): bisherige naturorientierte Grün/Sand/Orange-Palette ersetzt durch neutrale Graustufen + Systemblau als Primärfarbe (analog iOS/macOS "System Blue"), Systemgrün als Akzent, zusätzliche Chart-Farben (Orange/Violett/Türkis) angelehnt an Apples Health-/Fitness-App-Palette. `globals.css` (Light+Dark Mode), App-Icon (`public/icon.svg`, neu generiertes `favicon.ico`), PWA-Manifest- und Viewport-Theme-Color aktualisiert. Leaflet-Kartenfarben (Start/Ende/Marker-Farben in `track-map.tsx`) bewusst unverändert gelassen - sind semantische Track-Visualisierungsfarben, nicht Teil des Marken-Themes, und müssen wegen einer Leaflet-Eigenheit (siehe Code-Kommentar) ohnehin als feste Hex-Werte statt CSS-Variablen vorliegen. Typecheck, Lint, Build erfolgreich.

## Offener Punkt: IGP1-3 Punktaufteilung prüfen

Die Übungsnamen in IGP1-3 sind aus der PDF korrekt übernommen. Die genaue Punkteaufteilung innerhalb Abteilung B (Unterordnung) und Abteilung C (Schutzdienst) ließ sich aus der mehrspaltigen Tabellenlayout der PDF nicht immer zweifelsfrei pro Prüfungsstufe extrahieren - die aktuell hinterlegten Werte sind plausible Näherungen (mit `"Näherungswert"` markiert in den ScoringNotes) und sollten von dir als VDH-Vorstand vor einem Produktiveinsatz gegen die offizielle PO geprüft/korrigiert werden. Betroffen: `backend/src/Dogity.Infrastructure/Persistence/Seed/SportCatalogSeeder.cs`.

## Sicherheitshinweis (erledigt, dokumentiert)

Beim Versuch, eine PDF-Textextraktion per NuGet-Paket (`UglyToad.PdfPig`) einzubinden, fiel eine verdächtige Versionshistorie auf (Sprung von `0.1.9-alpha` auf `1.7.0-custom-5`, dünne Metadaten - typisches Muster für ein gekapertes Paket). Paket sofort entfernt, NuGet-Cache bereinigt, nichts davon wurde je gebaut/ausgeführt. Stattdessen wird das etablierte externe Tool `pdftotext` (poppler-utils) aufgerufen - siehe DEPLOYMENT.md.

Zusätzlich: Ein in README.md im Klartext committetes lokales Dev-DB-Passwort wurde gefunden (war bereits zu GitHub gepusht), aus der Doku entfernt und das Passwort rotiert.

## Offene Punkte (Frontend/Fährte)

- [x] Dropdowns gestylt: native `<select>`-Elemente durch eine selbst gerenderte Select-Komponente (`components/ui/select.tsx`, auf Basis von `@base-ui/react/select`, analog zu `components/ui/button.tsx`/`sheet.tsx`) ersetzt - in allen acht betroffenen Dateien (`admin/page.tsx`, `dogs/page.tsx`, `dogs/[id]/page.tsx`, `trainer/page.tsx`, `global-exercises-section.tsx`, `regulation-import-section.tsx`, `goals-section.tsx`, `club-exercises-section.tsx`). Typecheck, Lint und `next build` (alle Routen inkl. der dynamischen) laufen fehlerfrei durch; visuell im Browser noch nicht gegengeprüft, da der Chrome-Zugriff in dieser Session nicht verfügbar war.
- [x] Fährtenaufzeichnung: GPS-Aufzeichnungsdichte erhöht - `maximumAge` in `fahrte-recorder.tsx` und `gps-track-section.tsx` von `1000` auf `0` gesetzt (erzwingt bei jedem `watchPosition`-Tick eine frische Messung statt einer ggf. zwischengespeicherten) und als benannte Konstante `GPS_MAX_POSITION_AGE_MS` zentral anpassbar gemacht. Die eigentliche Tick-Frequenz von `watchPosition` ist Browser-/Hardware-gesteuert und von JS aus nicht weiter erhöhbar - `maximumAge: 0` ist der wirksamste verfügbare Hebel.
- [x] Fährtenaufzeichnung: manuelle Markierungspunkte für gelegte Gegenstände (separat von den automatischen GPS-Punkten) umgesetzt. Domain: `GpsPoint` um `PointType` (Automatic/Manual) + optionales `Label` erweitert (Migration `AddGpsPointTypeAndLabel`, angewendet). Frontend: in `fahrte-recorder.tsx` und `gps-track-section.tsx` ein "Punkt setzen"-Button mit optionalem Label-Feld während der Aufnahme (per `getCurrentPosition`, unabhängig vom laufenden `watchPosition`); `track-map.tsx` zeigt manuelle Marker als eigene orangene Punkte mit Tooltip, sie fließen nicht in Linie/Start/Ende/Streckenschätzung ein. Backend, Frontend-Build (inkl. aller Routen) und End-to-End-Test per curl (Track mit einem automatischen und einem manuellen Punkt anlegen/abrufen) erfolgreich.
- [x] Fährte erneut ablaufen können umgesetzt (Entscheidung mit Auftraggeber: zweite Aufzeichnung am selben `GpsTrack`, nicht zwei verknüpfte Tracks). Domain: neue Entities `GpsWalkRun`/`GpsWalkPoint` (1..n Ablauf-Versuche pro `GpsTrack`, eigene Tabellen `gps_walk_runs`/`gps_walk_points`, Migration `AddGpsWalkRuns` angewendet). Backend: `POST /api/gps-tracks/{id}/walk-runs`, `GpsTrackDto` liefert `walkRuns` mit. Frontend: neue Komponente `walk-run-recorder.tsx` ("Fährte erneut ablaufen"-Button je Track in `gps-track-section.tsx`), `track-map.tsx` zeichnet jeden Ablauf-Versuch als gestrichelte Linie in eigener Farbe zum Vergleich mit der gelegten Fährte (durchgezogen). Backend-Tests, Frontend-Build (alle Routen) und End-to-End-Test per curl (Track + Ablauf-Versuch anlegen, beide über `GET /api/gps-tracks` mit `walkRuns` zurückbekommen) erfolgreich.
- [x] Geprüft: mehr als eine Fährte pro Training ist bereits möglich - `GpsTrackSection` erlaubt jederzeit eine neue Aufnahme für dieselbe `trainingSessionId`, das Backend hat keine Eindeutigkeitsbeschränkung. End-to-end per curl verifiziert (zwei `GpsTrack`s für dieselbe Trainingssession angelegt, beide werden über `GET /api/gps-tracks?trainingSessionId=...` zurückgegeben). War vermutlich aus einer früheren Projektphase als offen vermerkt und nie erneut geprüft worden.

## Offene Punkte (Rollenmodell)

- [x] Mehrere Rollen pro Person + Rollenswitch umgesetzt (Entscheidung mit Auftraggeber: rein datengetrieben statt über Identity-Rollen - "Trainer-Sein" ergibt sich automatisch aus mind. einer geleiteten Gruppe oder Vereins-Trainer-Zuweisung, kein manuelles Rollen-Zuweisen nötig). Backend: `GroupService.IsTrainerAsync` + neuer Endpoint `GET /api/groups/my-trainer-status`. Frontend: `AuthContext` ruft den Endpoint einmal nach Login ab und stellt `isTrainer` bereit; `sidebar-nav.tsx`/`bottom-nav.tsx` zeigen den "Trainer"-Navigationspunkt jetzt nur noch, wenn er tatsächlich zutrifft (vorher für jeden sichtbar, auch ohne jede Gruppe/Vereinszuweisung - ein eigener Bug). Der Wechsel zwischen Perspektiven ist die normale Navigation zwischen den jetzt korrekt gefilterten Menüpunkten (Mitglied immer, Trainer/Admin nur wenn zutreffend) - kein zusätzlicher Toggle nötig. Backend-Tests, Frontend-Build (alle Routen) und End-to-End-Test per curl (`/api/groups/my-trainer-status` für alle drei Demo-Rollen: `mitglied1`→false, `trainer`→true, `admin`→false) erfolgreich.

## Offene Punkte (Performance, niedrige Priorität)

Im Rahmen eines vollständigen Performance-Audits identifiziert, aber bewusst zurückgestellt, da der Nutzen beim aktuellen Datenvolumen (Demo-Daten, einstellige bis niedrige zweistellige Nutzerzahl) den Implementierungsaufwand noch nicht rechtfertigt - sobald die Nutzerzahl wächst, hier zuerst ansetzen:

- [ ] `GET /api/admin/users` liefert alle Nutzer ohne Paging zurück - bei großer Nutzerzahl ein wachsendes Payload-Problem für die Admin-Übersicht (aktuell nur eine Handvoll Demo-Nutzer, daher kein akutes Problem).
- [ ] `global-exercises-section.tsx`/`club-exercises-section.tsx` laden alle für den Nutzer sichtbaren Übungen und filtern client-seitig auf `clubId === null` bzw. den gewählten Verein - ein Query-Parameter am bestehenden `/api/sports/{sportId}/exercises`-Endpoint würde das vermeiden, spart aber nur wenige KB pro Aufruf in der Admin/Trainer-Verwaltung (kein Endnutzer-Pfad auf dem Hundeplatz).

## Aktuell keine weiteren offenen Punkte (außer den oben gelisteten)

Alle bisher beauftragten Features sind umgesetzt und verifiziert (Build, Lint, Typecheck, End-to-End-Tests per curl). Nächste sinnvolle Kandidaten laut ROADMAP.md Phase 3 "Vereinsplattform": Termine/Veranstaltungen, Prüfungsanmeldung. Folgen auf Zuruf.
