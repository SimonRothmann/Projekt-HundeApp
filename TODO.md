# Dogity – Entwicklungs-TODO

Wird laufend von Claude gepflegt, damit jederzeit ein Überblick über den aktuellen Stand besteht. Spiegelt die interne Task-Liste.

## Erledigt

- [x] Git-Repo initialisieren
- [x] Doku-Dateien optimieren (TECH_STACK.md, DEPLOYMENT.md, README.md – Hosting ohne CI/CD-Pflicht)
- [x] Backend-Skeleton: ASP.NET Core 9 Clean Architecture
- [x] Core-Domainmodell + EF Core Migration
- [x] Frontend-Skeleton: Next.js + TypeScript + Tailwind + shadcn
- [x] Lokale Dev-Umgebung ohne Docker
- [x] Trainingstagebuch: Application-Service, API-Controller, Frontend-UI
- [x] Zielplanung: Domain, Migration, Application-Service + Plan-Generator, API-Controller, Frontend-UI
- [x] Community-Domain: Club/Group/Membership (Grundmodell)
- [x] Sportkatalog erweitert: Bewertungskriterien je Übung + drei Fährten-Prüfungsstufen (A/B/C)
- [x] GPS/Fährte-Modul: Domain + API + Frontend (Browser-Geolocation, Leaflet/OSM-Karte)
- [x] Trainer-Übersicht: Gruppen anlegen/verwalten, Mitglieder einladen, individuelle Pläne zuweisen
- [x] Admin-Übersicht (Plattform-Scope): Kennzahlen, Nutzerliste, Prüfungsordnungs-Metadaten pflegen
- [x] PWA/Offline-Unterstützung: Manifest, Service Worker, IndexedDB-Sync-Queue für Training/GPS
- [x] Vereinsverwaltung (Backend + Frontend): Admin legt Vereine an und weist Trainern die Vereins-Rolle zu (ClubTrainer); Trainer können Gruppen optional einem zugewiesenen Verein zuordnen
- [x] Übungs-Verwaltung (Backend + Frontend): Admin pflegt globale Übungen, Vereinstrainer ausschließlich vereinsspezifische Übungen ihres Vereins (Exercise.ClubId + Sichtbarkeitsfilter, per curl/End-to-End verifiziert)
- [x] Prüfungsordnung-Import (Backend + Frontend): Scan der lokalen PDF via `pdftotext`, Admin wählt einzelne Vorschläge gezielt aus und übernimmt sie in den Katalog
- [x] Trainer-Feedback zu Trainingseinheiten (Backend + Frontend): nur ein per TrainerAssignment zugewiesener Trainer kann Feedback zu einem Training eines betreuten Hundes geben, der Besitzer sieht es nur lesend (schließt die letzte offene Lücke aus ROADMAP.md Phase 2 "Trainer Plattform")
- [x] Fährtenaufzeichnung im Frontend prominenter gemacht: direkter "Fährte aufnehmen"-Button auf der Hundedetailseite (legt Training + GPS-Track in einem Schritt an, auch offline), statt vorher zwingend ein vollständiges bewertetes Training anlegen zu müssen
- [x] Testaccounts + Demo-Daten für alle Rollen (`DemoDataSeeder`, läuft automatisch in Development): `admin@dogity.test`, `trainer@dogity.test`, `mitglied1@dogity.test`, `mitglied2@dogity.test`, Passwort jeweils `Demo1234!`
- [x] Prüfungsordnung-Import gegen die echte PDF ausgeführt und validiert (Scan + Übernahme einzelner Kandidaten end-to-end per curl getestet)
- [x] IGP1-3 (FCI-Internationale Gebrauchshundeprüfung) als neue Sportarten im Katalog angelegt, mit Übungsnamen und Punktzahlen direkt aus der UTI-REG-IGP-de-2025-PDF (Ausnahme von der sonst geltenden "keine Originaltexte"-Regel, ausdrücklich genehmigt durch den Auftraggeber in seiner Funktion als VDH-Vorstand)
- [x] Lokale Entwicklungsumgebung auf macOS aufgesetzt (Postgres@17, poppler, .NET 9 zusätzlich zu .NET 10 via Homebrew, DB-Rolle/Datenbank/Secrets, Migrationen, Backend+Frontend erfolgreich gebaut/gestartet, End-to-End-Login per curl verifiziert). macOS-spezifische Eigenheiten (Postgres-Start-Bug, .NET-Mehrversionsbetrieb) in README.md "Besonderheiten auf macOS" dokumentiert, Start/Stop-Skripte unter `scripts/db-start.sh`/`scripts/db-stop.sh`. Fehlende `frontend/.env.local.example` (von README referenziert, aber nie committet) ergänzt.
- [x] Resilienz: Service Worker (`frontend/public/sw.js`) cachte alle Same-Origin-GET-Requests (außer Navigation) cache-first mit einem statischen, nie versionierten `CACHE_NAME` - nach einem Produktions-Deployment blieben dadurch alte Inhalte (RSC-Payloads, Icons, Manifest) potenziell unbegrenzt im Cache, sichtbar als "App zeigt veraltete Inhalte", behebbar bisher nur durch manuelles Löschen der Browserdaten. Strategie umgestellt: nur inhaltsbasiert gehashte `/_next/static/`-Assets bleiben cache-first (sicher, da Hash sich mit Inhalt ändert), alles andere ist jetzt network-first mit Cache nur als Offline-Fallback; `CACHE_NAME` auf `v2` angehoben, um einmalig alte Fehlversionen zu invalidieren.
- [x] Resilienz: Frontend behandelte abgelaufene/invalide JWTs nicht - nach Tokenablauf blieb die App in einem kaputten Zustand (jeder weitere Request scheiterte mit 401, ohne dass die UI reagierte). `frontend/src/lib/api.ts` erkennt jetzt 401-Antworten auf authentifizierte Requests (Token war gesetzt), räumt `localStorage` auf und leitet automatisch zu `/login` weiter; ein 401 ohne Token (z.B. falsches Passwort beim Login) bleibt unverändert eine normale Formularfehlermeldung.
- [x] Performance: kein Application-Service nutzte `AsNoTracking()` - auch reine Lese-Endpunkte (Ziele, Trainingstagebuch, Hunde, Sportkatalog, GPS-Tracks, Gruppen, Vereine) haben Entities unnötig im EF-Core-ChangeTracker gehalten. Gezielt auf Lesepfade ergänzt, die nicht im selben Methodenaufruf speichern (Schreibpfade unverändert, ggf. per `track`-Parameter getrennt, z.B. `GoalService.GetOwnedGoalAsync`, `TrainingService.GetOwnedSessionAsync`). Backend-Tests weiterhin grün.
- [x] Performance: Response-Compression (Brotli/Gzip) im Backend aktiviert (`Program.cs`) - reduziert Payload-Größe für Trainingslisten/GPS-Tracks auf mobilen Verbindungen spürbar, per curl verifiziert (`Content-Encoding: br`).
- [x] Bugfix `ExerciseDifficulty`-Typ-Mismatch: Backend serialisiert das Enum mangels `JsonStringEnumConverter` als Zahl (`"difficulty": 1`), Frontend hatte es aber als String-Union (`"Beginner"|"Intermediate"|"Advanced"`) typisiert - dadurch lieferte `difficultyLabel[ex.difficulty]` in vier Dateien (`dogs/[id]/page.tsx`, `sports/page.tsx`, `global-exercises-section.tsx`, `club-exercises-section.tsx`) zur Laufzeit `undefined` statt des deutschen Labels. `types.ts` auf `0 | 1 | 2` umgestellt (analog zu `DogGender`/`GoalStatus`), alle vier `difficultyLabel`-Records auf numerische Keys angepasst; in den beiden Select-Formularen zusätzlich `Number(value)`-Konvertierung ergänzt, da `Object.entries()` über die Record-Keys immer Strings liefert - sonst wäre beim Anlegen/Bearbeiten einer Übung ein String statt einer Zahl ans Backend gesendet worden. Typecheck, Lint, Build und End-to-End-Verifikation per curl erfolgreich.
- [x] Performance (mobil): GPS-Track-Payload für lange Fährten reduziert. Lange Aufzeichnungen (1 Punkt/Sekunde, siehe `GPS_MAX_POSITION_AGE_MS`) konnten tausende Punkte unkomprimiert über mobile Verbindungen ausliefern und client-seitig mit Leaflet rendern. Neuer `GpsTrackSimplifier` (Douglas-Peucker, 3m Toleranz - innerhalb der ohnehin üblichen Smartphone-GPS-Genauigkeit, ab 200 Punkten aktiv) wird beim Lesen (`GetByTrainingSessionAsync`) auf automatische Punkte angewendet; manuelle Marker und die in der DB gespeicherten Rohpunkte bleiben unverändert. 4 neue Unit-Tests (`GpsTrackSimplifierTests`), End-to-End per curl verifiziert: 500 Testpunkte auf einer Geraden mit einem bewussten Ausreißer wurden beim Abruf auf 5 Punkte reduziert (Anfang, Ende, Ausreißer korrekt erhalten).
- [x] Code-Verschlankung (Frontend): `haversineMeters`/`estimateLengthMeters` waren dreifach identisch dupliziert (`fahrte-recorder.tsx`, `gps-track-section.tsx`, `walk-run-recorder.tsx`) - jetzt zentral in `lib/geo.ts`. Der komplette GPS-Aufzeichnungs-Lifecycle (watchPosition/getCurrentPosition/clearWatch inkl. Cleanup beim Unmount) war ebenfalls in allen drei Komponenten nahezu identisch dupliziert - in den neuen Hook `lib/use-gps-recorder.ts` ausgelagert. Das vierfach duplizierte `difficultyLabel`-Record (`dogs/[id]/page.tsx`, `sports/page.tsx`, `global-exercises-section.tsx`, `club-exercises-section.tsx`) liegt jetzt zentral in `lib/constants.ts`. Typecheck, Lint, Build erfolgreich; Verhalten unverändert (manuell durchgesteuert: Aufnahme starten/markieren/stoppen je Komponente).
- [x] Bugfix Smartphone-Zugriff über LAN: Login (und jede andere Formular-Interaktion) funktionierte beim Aufruf über die LAN-IP des Entwicklungsrechners nicht - die Seite lud, aber Formulare lösten beim Absenden einen normalen Browser-Reload statt des JS-Submit-Handlers aus, Buttons reagierten nicht. Ursache: Next.js 15+ blockt seit einer Sicherheitsänderung standardmäßig Cross-Origin-Zugriffe auf Dev-Server-Assets (JS-Chunks/HMR) - das initiale HTML wird noch ausgeliefert, React hydratisiert aber nie, weil die Chunks mit 403 abgewiesen werden (sichtbar nur im Server-Log, nicht im Browser). Backend (CORS, Kestrel-Bind auf `0.0.0.0`) und macOS-Firewall waren bereits korrekt konfiguriert und nicht die Ursache. Fix: `allowedDevOrigins: ["192.168.1.*"]` in `frontend/next.config.ts` (Wildcard statt fester IP, damit eine andere DHCP-vergebene Adresse im selben Subnetz nicht erneut bricht). End-to-End auf echtem Smartphone verifiziert.
- [x] Bugfix GPS auf iPhone: Fährtenaufzeichnung schlug mit "keine Erlaubnis" fehl, **ohne dass der iOS-Berechtigungsdialog überhaupt erschien**. Ursache: Safari erlaubt `navigator.geolocation` nur in einem Secure Context (HTTPS) - außerhalb von `localhost` ohne Ausnahme, anders als z.B. Chrome auf Android/Desktop, das private LAN-IPs großzügiger behandelt. Die App lief bisher nur über `http://<LAN-IP>:3000`. Lösung: lokales HTTPS-Setup per `mkcert` (kostenlos, bleibt komplett im eigenen Netz, Alternative zu einem öffentlichen Tunnel-Dienst wäre möglich gewesen, aber bewusst nicht gewählt). Backend nutzt jetzt das `https`-Profil (Port 7297) mit dem mkcert-Zertifikat über `Kestrel:Certificates:Default` in `appsettings.Development.json`; Frontend hat ein neues Skript `npm run dev:https` (Next.js' `--experimental-https-key/-cert`). `lib/api.ts` wählt den Backend-Port jetzt anhand des Protokolls der aufrufenden Seite (`7297` bei HTTPS, `5080` bei HTTP) statt ihn aus der konfigurierten URL abzuleiten - sonst wäre der API-Call vom HTTPS-Frontend aus auf den falschen (reinen HTTP-)Port gegangen. Setup-Anleitung (inkl. einmaligem Zertifikat-Vertrauen auf dem iPhone per AirDrop) in README.md "GPS/Fährtenaufzeichnung vom iPhone testen". End-to-End auf echtem iPhone verifiziert: Standortdialog erscheint jetzt, Aufzeichnung läuft.
- [x] Fährtenaufzeichnung präzisiert (Winkel/Abbiegungen sichtbar, Live-Karte): Drei zusammenhängende Probleme behoben. (1) `GpsTrackSimplifier`-Toleranz war mit 3m zu großzügig - bei Schrittgeschwindigkeit (~1m zwischen Punkten) liegt eine scharfe Abbiegung oft nur 0,5-1m von der Geraden zwischen Vorgänger/Nachfolger entfernt und wurde dadurch weggeglättet; auf 1m Toleranz reduziert und die Schwelle, ab der überhaupt vereinfacht wird, von 200 auf 2000 Punkte angehoben (reguläre Trainingstracks bleiben jetzt komplett unverändert, die Vereinfachung greift nur noch als Sicherheitsnetz für pathologisch lange Aufzeichnungen). (2) `watchPosition()` hat keinen Parameter für die Abtastrate und wird von manchen Betriebssystemen (u.a. beobachtet auf iOS) zusätzlich aus Energiespargründen gedrosselt - `lib/use-gps-recorder.ts` nutzt jetzt einen eigenen Poll-Loop mit `getCurrentPosition()` alle 500ms (rekursives `setTimeout`, keine überlappenden Anfragen bei schlechtem Empfang), das erzwingt den gewünschten Takt, schneller als das tatsächliche Update-Intervall des GPS-Chips (üblich ca. 1 Hz) bringt das aber naturgemäß keine zusätzlichen echten Punkte. (3) Während der Aufnahme wurde bisher gar keine Karte angezeigt, nur ein Punktezähler - `TrackMap` hat jetzt einen `live`-Modus: Karte wird einmalig erzeugt und bei neuen Punkten nur noch inkrementell aktualisiert (kein Neuaufbau pro GPS-Tick, das hätte sonst geruckelt), Kartenmitte folgt per `panTo` dem aktuellen Standort bei festem Zoom-Level 18. Eingebunden in `fahrte-recorder.tsx`, `gps-track-section.tsx` und `walk-run-recorder.tsx`. Backend-Tests (Schwellwerte angepasst), Typecheck, Lint, Build erfolgreich.

- [x] Rebrand "CanisTrack" → "Dogity": komplette Umbenennung über das gesamte Repository - 6 Backend-Projekte (Verzeichnisse, `.csproj`, Namespaces/`using`, `.sln`), alle Dokumentations-Dateien, Frontend (Titel, Manifest, Service Worker, localStorage-Keys, API-Lib-Kommentare), lokale Postgres-Datenbank/Rolle (`canistrack_dev`/`canistrack` → `dogity_dev`/`dogity` per `ALTER DATABASE`/`ALTER ROLE`, Daten bleiben erhalten) und Demo-Account-E-Mail-Domain (`@dogity.test`). Backend-Build+Tests sowie Frontend-Typecheck/Lint/Build erfolgreich, Login mit umbenanntem Demo-Account per curl verifiziert.
- [x] Farbschema auf Apple-orientierte Palette umgestellt (siehe DESIGN_SYSTEM.md "Farben"): bisherige naturorientierte Grün/Sand/Orange-Palette ersetzt durch neutrale Graustufen + Systemblau als Primärfarbe (analog iOS/macOS "System Blue"), Systemgrün als Akzent, zusätzliche Chart-Farben (Orange/Violett/Türkis) angelehnt an Apples Health-/Fitness-App-Palette. `globals.css` (Light+Dark Mode), App-Icon (`public/icon.svg`, neu generiertes `favicon.ico`), PWA-Manifest- und Viewport-Theme-Color aktualisiert. Leaflet-Kartenfarben (Start/Ende/Marker-Farben in `track-map.tsx`) bewusst unverändert gelassen - sind semantische Track-Visualisierungsfarben, nicht Teil des Marken-Themes, und müssen wegen einer Leaflet-Eigenheit (siehe Code-Kommentar) ohnehin als feste Hex-Werte statt CSS-Variablen vorliegen. Typecheck, Lint, Build erfolgreich.
- [x] Farbkontrast/Übergänge entschärft + App-Icon verfeinert: Feedback "zu blockig, zu hartes Schwarz auf Schwarz". Light/Dark-Mode-Kontrast reduziert (kein reines Schwarz-auf-Weiß/Weiß-auf-Schwarz mehr), Dark Mode nutzt gestufte Dunkelgrautöne statt nahezu reinem Schwarz, alle Farb-/Randübergänge (Hover, Theme-Toggle) blenden weich über. App-Icon mit Verlauf statt Flat-Color, iOS-typischerer Eckenradius.
- [x] Select-Dropdowns zeigten nach Auswahl die rohe Id statt des Labels: Ursache war `@base-ui/react`'s `Select.Value`, das ein Label nur auflöst, wenn dem `Select.Root` eine explizite `items`-Liste `{value, label}` übergeben wird - ohne sie wird der rohe `value` (bei uns meist eine GUID) angezeigt. `components/ui/select.tsx` extrahiert die Liste jetzt automatisch aus den als Kindern übergebenen `<SelectItem>`-Elementen, betrifft alle ca. 12 Selects im Projekt ohne Änderung an den Verwendungsstellen.
- [x] "App reconnected ständig und erreicht den Server nicht" beim Offline-Test der Fährtenaufzeichnung: kein Bug in der Offline-Logik selbst, sondern `npm run dev`/`dev:https` wurde zum Testen verwendet - Next.js' Dev-Client hält für Hot-Reload eine WebSocket-Verbindung offen und versucht bei Verbindungsverlust ständig neu zu verbinden/neu zu laden, zusätzlich registriert sich der Service Worker (App-Shell-Cache) laut `pwa-register.tsx` nur bei `NODE_ENV=production` und wird im Dev-Modus aktiv deinstalliert. Neuer `npm run start:https` (`frontend/server.mjs`, Custom-HTTPS-Server für den Produktions-Build mit denselben mkcert-Zertifikaten wie `dev:https`, da `next start` kein `--experimental-https` kennt) ermöglicht echtes Offline-Testen gegen einen Produktions-Build. Dokumentiert in README.md "Echtes Offline-Verhalten testen".
- [x] Service Worker registriert sich jetzt auch im Dev-Modus (vorher nur Production) - Nutzer-Feedback "Dev und Production sollen sich identisch verhalten, sonst macht Dev keinen Sinn". `public/sw.js` unterscheidet per `?env=...`-Query-String an der Registrierungs-URL (wird laut Spec Teil von `self.location` im Worker, daher ohne Build-Schritt für die unverarbeitet aus `public/` ausgelieferte Datei möglich) nur noch in einem Punkt zwischen Dev und Production: `/_next/`-Build-Assets werden im Dev-Modus nicht gecacht, weil sie dort - anders als im Production-Build - nicht inhaltsbasiert-stabil sind und cache-first veraltete Chunks ausliefern sowie Hot Reload/Turbopacks Hot-Reload-Streaming-Endpoint kaputt machen würde. Die eigentlich relevante Offline-Fähigkeit (Navigation übersteht Verbindungsverlust, Fallback auf `/offline`, IndexedDB-Schreibwarteschlange) ist in beiden Modi jetzt identisch aktiv. `CACHE_NAME` auf "v3" angehoben, damit alte Caches sauber ersetzt werden.
- [x] Trainingsplan-Feature überarbeitet: Nutzer-Feedback "zu bescheiden gelöst, man sollte sehen welche Übungen gemacht wurden + Kommentare, und mehr als 1x/Woche mit mehreren Übungen trainieren können". Bisher waren Plan (Goal/TrainingPlan/TrainingPlanItem) und Tagebuch (TrainingSession/TrainingExercise) zwei komplett getrennte Systeme - der Plan hatte nicht mal ein "erledigt"-Feld in der DB, nur eine Übung pro Woche, keine Kommentare. Statt einer zweiten, parallelen "erledigt"-Verwaltung wurden beide Systeme verknüpft: `TrainingExercise` hat jetzt ein optionales `TrainingPlanItemId` (FK, `OnDelete SetNull`, neue Migration `AddTrainingPlanItemLinkToTrainingExercise`) - ein Tagebucheintrag kann damit als Erfüllung eines Wochenziels markiert werden. `TrainingPlanGenerator` erzeugt jetzt bis zu 2 verschiedene Übungs-Ziele pro Woche statt nur einer (`ItemsPerWeek`). `GoalService` liefert pro Plan-Ziel jetzt `completedCount`/`isComplete`/`logs` (Datum, Bewertung, Erfolg, Kommentar der verknüpften Tagebucheinträge) - der Fortschritt ergibt sich also direkt aus echten Trainingseinträgen statt aus einem separat zu pflegenden Häkchen. `TrainingService` validiert beim Anlegen eines Tagebucheintrags, dass ein referenziertes Plan-Ziel zum selben Hund und zur selben Übung gehört. Frontend: `goals-section.tsx` zeigt Wochen mit mehreren Zielen, Fortschritt (✓/○, "2/3x erledigt") und die verknüpften Log-Einträge inkl. Kommentar an; `dogs/[id]/page.tsx` bekommt im Trainingstagebuch-Formular pro Übungszeile ein optionales "Plan-Ziel"-Dropdown (nur sichtbar, wenn ein aktives Ziel zur gewählten Übung passt). Backend-Tests (2 neue für den Generator), Frontend-Typecheck/Lint/Build sowie ein manueller Smoketest per curl (Plan erzeugen → Training mit Plan-Ziel-Bezug speichern → Fortschritt im Plan korrekt erhöht, Validierung von Übung/Plan-Ziel-Mismatch greift) erfolgreich.
- [x] Nutzer-Feedback nach erstem Test: "kann Plan-Ziele nicht selbstständig auswählen, nur alle als erledigt markieren". Ursache war ein echter Design-Fehler in der ersten Version - die Plan-Ziele in `goals-section.tsx` waren rein informativ, einzig interaktiv war der Goal-weite Button "Als erreicht markieren" (markiert das ganze Ziel, nicht ein einzelnes Wochenziel). Jeder Plan-Ziel-Eintrag ist jetzt selbst klickbar und öffnet ein kompaktes Inline-Formular (Bewertung, Erfolg-Checkbox, Kommentar, "Eintragen") - speichert direkt einen minimalen Tagebucheintrag mit Verknüpfung zu diesem Plan-Ziel, ohne den Umweg über das volle Trainingstagebuch-Formular. Zusätzlich `components/ui/select.tsx`: die automatisch aus den Kind-Elementen extrahierte "items"-Liste (siehe Dropdown-Label-Fix) wird jetzt per `useMemo` statt bei jedem Render neu berechnet - Verdacht auf einen gemeldeten Lade-Loop, da durch die neuen Plan-Ziel-Dropdowns/Inline-Formulare deutlich mehr Select-Instanzen gleichzeitig im DOM stehen als vorher.
- [x] Trainingsplan-Generierung war "zufällig wirkend mit zu wenigen Übungen": Ursache gefunden - beim Zielanlegen wurde nur eine Sportart, keine konkrete Prüfungsordnung gewählt, der Generator rotierte daher blind über ALLE `Exercise`-Zeilen einer Sportart. Bei Sportarten mit mehreren Prüfungsordnungen (z.B. "Fährte" mit den Stufen A/B/C) mischte das Übungen verschiedener, fachlich unabhängiger Prüfungsstufen durcheinander - keine echte "Logik", wie der Nutzer richtig anmerkte. Jetzt: `Goal` hat ein optionales `RegulationId` (Migration `AddRegulationToGoal`), das Ziel-Formular zeigt nach Sportart-Auswahl eine "Prüfung"-Auswahl (z.B. "Fährte C (Fremdfährte)" statt nur "Fährte"), und `TrainingPlanGenerator` bekommt jetzt die Pflichtübungsliste (`RegulationExercise.IsMandatory`) der gewählten Prüfungsordnung statt aller Sportart-Übungen - garantiert, dass JEDE Pflichtübung vor dem Zieldatum mindestens einmal vorkommt (Pensum 1-4 Übungen/Woche richtet sich nach Pflichtübungsanzahl und verfügbarer Zeit statt einer festen Zahl). Ohne gewählte Prüfung Fallback auf alle Sportart-Übungen wie zuvor. Zusätzlich, wie vom Nutzer gewünscht, ist der Plan jetzt nachträglich bearbeitbar: neue Endpoints `POST/DELETE /api/goals/{id}/plan-items[/{itemId}]` erlauben, Übungen manuell zu einer beliebigen Woche hinzuzufügen (ersetzt einen reinen Pausenwochen-Platzhalter, falls vorhanden) oder zu entfernen - damit sind beliebig viele Übungseinheiten/Übungen pro Woche möglich, nicht nur die generierten. Backend-Tests erweitert (Pflichtübungs-Abdeckung, Kür-Übungen werden ignoriert), Typecheck/Lint/Build grün, manueller Smoketest per curl (Fährte-C-Ziel zeigt nur die 3 zugehörigen Übungen statt aller 5 Fährte-Übungen; Übung zu Pausenwoche hinzugefügt ersetzt den Platzhalter; falsche Sportart-Übung wird abgelehnt) erfolgreich.
- [x] Reload-Loop beim Öffnen der Trainingspläne, nur durch Löschen der Browserdaten behebbar: Ursache war die kurz zuvor eingeführte Service-Worker-Registrierung im Dev-Modus (siehe Eintrag weiter oben "Service Worker registriert sich jetzt auch im Dev-Modus") - `clients.claim()` übernimmt sofort auch bereits offene Tabs, der Service Worker hat dabei u.a. Next.js' RSC-/Datenabrufe bei der Client-seitigen Navigation abgefangen und gecacht, was den Next-Router aus dem Tritt brachte. Rückgängig gemacht: `pwa-register.tsx` registriert wieder nur in Production, im Dev-Modus wird ein ggf. vorhandener Worker aktiv entfernt (alter Zustand vor diesem Versuch). `public/sw.js` von der jetzt toten `?env=development`-Unterscheidung bereinigt, `CACHE_NAME` auf "v4" angehoben, damit betroffene Browser den fehlerhaften Cache beim nächsten Dev-Start automatisch loswerden. Echtes Offline-Testen bleibt wie vorher nur über den Produktions-Build (`npm run start:https`) möglich - der Versuch, das auch im Dev-Modus 1:1 nachzubilden, war es angesichts dieses Bugs nicht wert.
- [x] Prüfungsinhalte waren unvollständig ("haufenweise fehlende Bausteine", FCI-FH 1-3 komplett fehlend): Nutzer hat als VDH-Vorstand die offizielle FCI-Prüfungsordnung 2025 (UTI-REG-IGP-de-2025) bereitgestellt und explizit (analog zur bisherigen IGP1-3-Genehmigung) die Übernahme echter Übungsnamen/Punkte autorisiert. `SportCatalogSeeder.cs` korrigiert: **BH** - fehlende Pflichtübung "Ablegen unter Ablenkung" (5 Pkt) ergänzt, UND ein vorbestehender Fehler behoben, bei dem die echten Teil-A-Übungen (Leinenführigkeit/Sitz/Ablegen) fälschlich mit 0 statt 30/10/10 Punkten hinterlegt waren. **IBGH1-3** - bisherige Übungsnamen/Punkte waren komplett erfunden (z.B. "Fußarbeit"/"Abrufen" statt der echten "Leinenführigkeit"/"Freifolge"/...); durch neue RegulationVersion "2025" mit den echten FCI-IBGH-Übungen ersetzt (alte falsch benannte Version bleibt als harmloser Altbestand in der DB, wird aber nicht mehr verwendet). **IGP1-3** - fehlende Pflichtübungen ergänzt ("Ablegen in Verbindung mit Herankommen" fehlte komplett, "Revieren" fehlte in Abteilung C komplett), Abteilung-C-Übungen auf die echten Namen umbenannt (z.B. "Bewachen nach Rückkehr des Hundeführers" → "Abwehr eines Angriffs aus der Bewachungsphase"), alle "Näherungswert"-Punktzahlen durch die exakten Werte aus der PO ersetzt (neue RegulationVersion "2025-2"). **FCI-FH 1-3** (komplett neu) - drei neue Prüfungsordnungen für die Sportart "Fährte", nutzen dieselben Übungen wie Fährte A/B/C (Fährtenaufnahme/Winkelarbeit/Gegenstände verweisen), mit den echten Streckenlängen/Schenkeln/Winkeln/Fährtenaltern aus der PO; nur die Gegenstände-Punktzahl (21 Punkte je Stufe) ist exakt der offiziellen Tabelle entnommen, die Aufteilung Fährtenaufnahme/Winkelarbeit ist für Trainingszwecke vereinfacht (PO bewertet die Fährte als Gesamtleistung). Seeder-Hilfsfunktion erweitert: bestehende `RegulationExercise`-Zeilen werden jetzt bei jedem Start aktualisiert (vorher nur fehlende neu angelegt) - Korrekturen erreichen dadurch auch bereits gestartete lokale Entwicklungsdatenbanken ohne DB-Reset. Backend-Tests grün, Build erfolgreich, Live-Verifikation per curl (Fährte zeigt jetzt 6 Prüfungsordnungen inkl. FCI-IFH 1-3; IGP1/IBGH1/BH-Punktsummen exakt nachgerechnet; Plan-Generierung für FCI-IFH 3 liefert alle 3 zugehörigen Übungen zyklisch über die Wochen verteilt).
- [x] Prüfungsordnungs-Update wiederholbar gemacht (für künftige PO-Revisionen): neues [PRUEFUNGSORDNUNG_UPDATE.md](PRUEFUNGSORDNUNG_UPDATE.md) dokumentiert den Ablauf aus dem vorigen Punkt Schritt für Schritt (PDF lesen, Tabelle pro Prüfungsstufe bauen, Stolperfallen wie mehrfach vorkommende Übungsnamen, Versions-Supersession statt Überschreiben, Konsistenz-Skript, Verifikation). Vollautomatisches Parsing ist bewusst kein Ziel - die PDFs sind nicht einheitlich strukturiert genug, das Einordnen (Pflicht/Kür, welche Stufe, eigene Übung oder nur Bewertungsvariante) braucht Lesen mit Verständnis. Stattdessen `SportCatalogSeeder.cs` gehärtet: `SeedRegulationAsync` wirft jetzt eine `InvalidOperationException` mit Klartext-Fehlermeldung statt eine nicht gefundene Übungsreferenz stillschweigend zu überspringen (lief unbemerkt zur "Bausteine fehlen"-Lücke) - nur in Development aktiv, bricht nie Production. Von DATABASE.md "regulation_versions" verlinkt. Zusätzlich Font-Preload-Warnung behoben: `Geist_Mono` wurde global geladen/vorab angefordert, aber nirgendwo im Code tatsächlich verwendet (kein `font-mono` irgendwo) - ungenutzten Font-Import entfernt statt die Warnung zu ignorieren.
- [x] Nutzer-Hinweis "Abweichungen/Dopplungen in den Prüfungen" bestätigt und behoben: die vorigen Korrekturen (BH/IBGH/IGP) hatten die alten, falsch benannten Übungen bewusst nicht gelöscht (siehe PRUEFUNGSORDNUNG_UPDATE.md, ursprüngliche Fassung) - dadurch tauchten pro Sportart bis zu 10 verwaiste Alt-Übungen (z.B. "Fußarbeit" neben dem jetzt korrekten "Leinenführigkeit" bei IBGH) weiterhin in `/api/sports/{id}/exercises` auf, sichtbar u.a. im "Übung hinzufügen"-Dropdown im Trainingsplan. Vor dem Aufräumen per SQL verifiziert, dass keine der 32 betroffenen Übungen von echten `training_exercises`/`training_plan_items`-Zeilen referenziert wird (alle 0). `SportCatalogSeeder.cs` um `RemoveSupersededVersionAsync`/`RemoveOrphanedExercisesAsync` ergänzt (idempotent, am Ende von `SeedAsync` aufgerufen) - entfernt die durch neue Versionen abgelösten alten `RegulationVersion`-Zeilen (Cascade-Delete der zugehörigen `RegulationExercise`-Zeilen, gemeinsam weiterverwendete Übungen wie "Freifolge" bleiben unberührt) sowie danach komplett unreferenzierte Übungen. Eine legitime vereinsspezifische Übung ("Vereins-Begrüßungsübung", zwei Vereine, `ClubId` gesetzt) wurde bei der Prüfung korrekt nicht als Fehler erkannt und blieb unangetastet. PRUEFUNGSORDNUNG_UPDATE.md um den Aufräum-Schritt ergänzt, damit künftige Revisionen denselben Prozess befolgen. Live verifiziert: von 33 zuvor verwaisten Übungen bleibt nur noch die eine legitime vereinsspezifische übrig.

## Erledigt: IGP1-3 Punktaufteilung gegen offizielle PO verifiziert (2026-07-16)

Die komplette Punktaufteilung IGP 1-3 (Abteilung A S. 36, Abteilung B S. 44, Abteilung C S. 56) wurde seitenweise gegen das offizielle PDF UTI-REG-IGP-de-2025 (gültig ab 01.01.2025, vom Auftraggeber bereitgestellt) abgeglichen: **alle Punktwerte stimmen exakt**. Drei Beschreibungsfehler dabei gefunden und korrigiert: "Ablegen unter Ablenkung" hatte bei IGP 1 die IGP-3-Distanz (30 m/außer Sicht statt korrekt 10 m in Sichtweite seitwärts, PO S. 53); IGP 2 präzisiert (20 m, Rücken zum Hund); Rückentransport-Beschreibungen präzisiert (30 Schritte + anschließender Seitentransport bzw. Übergang in den Überfall).

## Sicherheitshinweis (erledigt, dokumentiert)

Beim Versuch, eine PDF-Textextraktion per NuGet-Paket (`UglyToad.PdfPig`) einzubinden, fiel eine verdächtige Versionshistorie auf (Sprung von `0.1.9-alpha` auf `1.7.0-custom-5`, dünne Metadaten - typisches Muster für ein gekapertes Paket). Paket sofort entfernt, NuGet-Cache bereinigt, nichts davon wurde je gebaut/ausgeführt. Stattdessen wird das etablierte externe Tool `pdftotext` (poppler-utils) aufgerufen - siehe DEPLOYMENT.md.

Zusätzlich: Ein in README.md im Klartext committetes lokales Dev-DB-Passwort wurde gefunden (war bereits zu GitHub gepusht), aus der Doku entfernt und das Passwort rotiert.

## Offene Punkte (Frontend/Fährte)

- [x] Dropdowns gestylt: native `<select>`-Elemente durch eine selbst gerenderte Select-Komponente (`components/ui/select.tsx`, auf Basis von `@base-ui/react/select`, analog zu `components/ui/button.tsx`/`sheet.tsx`) ersetzt - in allen acht betroffenen Dateien (`admin/page.tsx`, `dogs/page.tsx`, `dogs/[id]/page.tsx`, `trainer/page.tsx`, `global-exercises-section.tsx`, `regulation-import-section.tsx`, `goals-section.tsx`, `club-exercises-section.tsx`). Typecheck, Lint und `next build` (alle Routen inkl. der dynamischen) laufen fehlerfrei durch; visuell im Browser noch nicht gegengeprüft, da der Chrome-Zugriff in dieser Session nicht verfügbar war.
- [x] Fährtenaufzeichnung: GPS-Aufzeichnungsdichte erhöht - `maximumAge` in `fahrte-recorder.tsx` und `gps-track-section.tsx` von `1000` auf `0` gesetzt (erzwingt bei jedem `watchPosition`-Tick eine frische Messung statt einer ggf. zwischengespeicherten) und als benannte Konstante `GPS_MAX_POSITION_AGE_MS` zentral anpassbar gemacht. Die eigentliche Tick-Frequenz von `watchPosition` ist Browser-/Hardware-gesteuert und von JS aus nicht weiter erhöhbar - `maximumAge: 0` ist der wirksamste verfügbare Hebel.
- [x] Fährtenaufzeichnung: manuelle Markierungspunkte für gelegte Gegenstände (separat von den automatischen GPS-Punkten) umgesetzt. Domain: `GpsPoint` um `PointType` (Automatic/Manual) + optionales `Label` erweitert (Migration `AddGpsPointTypeAndLabel`, angewendet). Frontend: in `fahrte-recorder.tsx` und `gps-track-section.tsx` ein "Punkt setzen"-Button mit optionalem Label-Feld während der Aufnahme (per `getCurrentPosition`, unabhängig vom laufenden `watchPosition`); `track-map.tsx` zeigt manuelle Marker als eigene orangene Punkte mit Tooltip, sie fließen nicht in Linie/Start/Ende/Streckenschätzung ein. Backend, Frontend-Build (inkl. aller Routen) und End-to-End-Test per curl (Track mit einem automatischen und einem manuellen Punkt anlegen/abrufen) erfolgreich.
- [x] Fährte erneut ablaufen können umgesetzt (Entscheidung mit Auftraggeber: zweite Aufzeichnung am selben `GpsTrack`, nicht zwei verknüpfte Tracks). Domain: neue Entities `GpsWalkRun`/`GpsWalkPoint` (1..n Ablauf-Versuche pro `GpsTrack`, eigene Tabellen `gps_walk_runs`/`gps_walk_points`, Migration `AddGpsWalkRuns` angewendet). Backend: `POST /api/gps-tracks/{id}/walk-runs`, `GpsTrackDto` liefert `walkRuns` mit. Frontend: neue Komponente `walk-run-recorder.tsx` ("Fährte erneut ablaufen"-Button je Track in `gps-track-section.tsx`), `track-map.tsx` zeichnet jeden Ablauf-Versuch als gestrichelte Linie in eigener Farbe zum Vergleich mit der gelegten Fährte (durchgezogen). Backend-Tests, Frontend-Build (alle Routen) und End-to-End-Test per curl (Track + Ablauf-Versuch anlegen, beide über `GET /api/gps-tracks` mit `walkRuns` zurückbekommen) erfolgreich.
- [x] Geprüft: mehr als eine Fährte pro Training ist bereits möglich - `GpsTrackSection` erlaubt jederzeit eine neue Aufnahme für dieselbe `trainingSessionId`, das Backend hat keine Eindeutigkeitsbeschränkung. End-to-end per curl verifiziert (zwei `GpsTrack`s für dieselbe Trainingssession angelegt, beide werden über `GET /api/gps-tracks?trainingSessionId=...` zurückgegeben). War vermutlich aus einer früheren Projektphase als offen vermerkt und nie erneut geprüft worden.

## Offene Punkte (Rollenmodell)

- [x] Mehrere Rollen pro Person + Rollenswitch umgesetzt (Entscheidung mit Auftraggeber: rein datengetrieben statt über Identity-Rollen - "Trainer-Sein" ergibt sich automatisch aus mind. einer geleiteten Gruppe oder Vereins-Trainer-Zuweisung, kein manuelles Rollen-Zuweisen nötig). Backend: `GroupService.IsTrainerAsync` + neuer Endpoint `GET /api/groups/my-trainer-status`. Frontend: `AuthContext` ruft den Endpoint einmal nach Login ab und stellt `isTrainer` bereit; `sidebar-nav.tsx`/`bottom-nav.tsx` zeigen den "Trainer"-Navigationspunkt jetzt nur noch, wenn er tatsächlich zutrifft (vorher für jeden sichtbar, auch ohne jede Gruppe/Vereinszuweisung - ein eigener Bug). Der Wechsel zwischen Perspektiven ist die normale Navigation zwischen den jetzt korrekt gefilterten Menüpunkten (Mitglied immer, Trainer/Admin nur wenn zutreffend) - kein zusätzlicher Toggle nötig. Backend-Tests, Frontend-Build (alle Routen) und End-to-End-Test per curl (`/api/groups/my-trainer-status` für alle drei Demo-Rollen: `mitglied1`→false, `trainer`→true, `admin`→false) erfolgreich.

## Offene Punkte (Performance, niedrige Priorität)

Im Rahmen eines vollständigen Performance-Audits identifiziert, aber bewusst zurückgestellt, da der Nutzen beim aktuellen Datenvolumen (Demo-Daten, einstellige bis niedrige zweistellige Nutzerzahl) den Implementierungsaufwand noch nicht rechtfertigt - sobald die Nutzerzahl wächst, hier zuerst ansetzen:

- [x] `GET /api/admin/users` liefert alle Nutzer ohne Paging zurück - erledigt: Endpoint hat inzwischen `page`/`pageSize`-Parameter (`AdminUserPageDto`, Default 50/Seite).
- [ ] `global-exercises-section.tsx`/`club-exercises-section.tsx` laden alle für den Nutzer sichtbaren Übungen und filtern client-seitig auf `clubId === null` bzw. den gewählten Verein - ein Query-Parameter am bestehenden `/api/sports/{sportId}/exercises`-Endpoint würde das vermeiden, spart aber nur wenige KB pro Aufruf in der Admin/Trainer-Verwaltung (kein Endnutzer-Pfad auf dem Hundeplatz).

- [x] Vier Trainingsplan/-tagebuch-Wünsche umgesetzt: **(1)** Abgebrochene/erreichte Ziele lassen sich jetzt löschen ("Ziel löschen"-Button, `window.confirm`-Bestätigung, ruft den schon vorhandenen DELETE-Endpoint). **(2)** Plan-Items lassen sich jetzt direkt bearbeiten statt nur entfernen+neu anlegen (neuer `PUT /api/goals/{id}/plan-items/{itemId}`-Endpoint, `UpdatePlanItemAsync`) - Woche und Zielwert änderbar, die Übung selbst bewusst nicht (sonst würden bereits verknüpfte Tagebucheinträge plötzlich zu einer anderen Übung gehören). **(3)** `TrainingPlanGenerator` plant jetzt mindestens 4 Übungen/Woche (Richtwert: 2 Einheiten x 2 Übungen) statt nur so viele wie nötig, um den Pflichtkatalog einmal abzudecken - skaliert nur darüber hinaus (bis max. 6), wenn ein großer Pflichtkatalog sonst nicht rechtzeitig vor dem Zieldatum vollständig untergebracht werden könnte. **(4)** Trainingstagebuch-Einträge können jetzt als Freitext statt als Katalog-Übung erfasst werden (für spontane Spaß-/Sonstige Übungen, die nicht Teil einer Prüfungsordnung sind) - `TrainingExercise.ExerciseId` ist jetzt nullable, neues `FreeTextLabel`-Feld (Migration `AddFreeTextExercises`), Frontend bekommt einen Umschalt-Button pro Übungszeile zwischen Katalog-Auswahl und Freitext-Eingabe. Dabei zwei echte Bugs gefunden und behoben: (a) die alten, beim IBGH/IGP-Korrekturdurchlauf ersetzten `RegulationSeed`-Blöcke ("Fußarbeit" etc.) referenzierten Übungen, die nicht mehr deklariert waren - mit der seit dem letzten Mal scharf geschalteten Fehlerprüfung crashte der Server beim Neustart; die toten Code-Blöcke wurden komplett entfernt (nicht nur ihre DB-Daten). (b) Ohne gewählte Prüfungsordnung zog der Generator versehentlich auch fremde, vereinsspezifische Übungen in den Plan (`Exercise.ClubId` wurde im Fallback-Pool nicht gefiltert) - teils sogar doppelt, da zwei Vereine identisch benannte Übungen angelegt hatten. Backend-Tests erweitert (Mindestpensum-Test), Typecheck/Lint/Build grün, alle vier Features live per curl verifiziert.

- [x] Docker-Compose-Setup für Prod+Test auf einem VPS vorbereitet (Oracle-Kapazitätsproblem hält weiter an, Nutzer sucht alternativen Server): `backend/Dockerfile` (Multi-Stage, ASP.NET-Runtime-Image + poppler-utils für den PDF-Import), `frontend/Dockerfile` (Multi-Stage, `NEXT_PUBLIC_API_URL` als Build-Arg, da zur Build-Zeit fest ins Client-Bundle eingebacken - node_modules werden 1:1 aus der Build-Stage übernommen statt mit `--omit=dev` neu installiert, da `next start` zur Laufzeit `next.config.ts` einliest und dafür ggf. `typescript` braucht), `docker-compose.yml` (eine gemeinsame Postgres-Instanz mit getrennten Datenbanken/Rollen für Prod/Test via `deploy/postgres-init/`, Caddy als gemeinsamer Reverse Proxy mit automatischem Let's-Encrypt-TLS für vier Subdomains), `scripts/deploy.sh` (rsync + Build direkt auf der VPS, kein Image-Registry nötig), `deploy/README.md` (Architektur, Ersteinrichtung, Backups). Dabei eine echte Lücke gefunden und behoben: Migrationen + Sportarten-/Übungskatalog liefen bisher nur in `IsDevelopment()`, eine Production-Instanz wäre also mit leerer Datenbank gestartet - jetzt laufen Migrationen/RoleSeeder/SportCatalogSeeder/AdminBootstrapper in allen Umgebungen, nur Swagger-UI und die erfundenen Demo-Daten bleiben auf Development beschränkt. "Test"-Umgebung läuft bewusst mit `ASPNETCORE_ENVIRONMENT=Development` (nicht extra "Staging"), um automatisch Demo-Daten zu bekommen, statt einen dritten Modus einzuführen. Backend-Tests grün, Dockerfiles/Compose lokal nicht gegen echten Docker getestet (kein Docker auf dem Entwicklungsrechner installiert) - vor dem ersten echten Deployment auf der Ziel-VPS verifizieren.

- [x] Sechs Komfort-Features umgesetzt: **(1) Passwort-Reset** (`POST /api/auth/forgot-password`/`reset-password`, nutzt `UserManager.GeneratePasswordResetTokenAsync`/`ResetPasswordAsync`; Versand über neue `IEmailSender`-Abstraktion - aktiv ist vorerst `LoggingEmailSender` (loggt statt zu verschicken), `SmtpEmailSender` mit MailKit ist fertig aber nicht registriert, da noch keine SMTP-Zugangsdaten vorliegen; Antwort ist bewusst immer gleich, egal ob die E-Mail existiert). **(2) Vereinsbeitritt mit Trainer-Freigabe**: neue Domain-Entity `ClubMembership` (Status Pending/Approved/Rejected, eine Tabelle statt Anfrage+Mitgliedschaft getrennt zu modellieren), neuer `ClubsController` (`GET /api/clubs` browsbare Liste, `POST .../join-requests`, `GET/POST .../join-requests/{id}/approve|reject` trainer-scoped). **(3) Trainer befördert Mitglieder zu Trainern**: `POST /api/clubs/{id}/members/{userId}/promote`, nur für bereits genehmigte Mitglieder des eigenen Vereins. **(4) Trainer-Sichtbarkeit**: neue `GET /api/clubs/{id}/members`/`join-requests`-Endpoints prüfen `IsClubTrainerAsync` vor jeder Antwort (404 bei fremdem Verein) - per curl verifiziert, dass ein Trainer Anfragen/Mitglieder eines fremden Vereins nicht sieht. **(5)+(6) Admin sperrt/löscht Nutzer**: `IUserLookupService` um `LockUserAsync`/`UnlockUserAsync`/`DeleteUserAsync` erweitert (Identity-Methoden `SetLockoutEndDateAsync`/`DeleteAsync`), `AuthController.Login` prüft jetzt zusätzlich `IsLockedOutAsync`; Löschen ist hart (kein FK zwischen Identity-User und Domain-`UserId`-Referenzen, verwaiste Zeilen zeigen "(unbekannt)" wie schon an anderer Stelle im Code). Frontend: `/forgot-password`+`/reset-password`-Seiten, neue `/clubs`-Übersicht (+Hinweiskarte im Dashboard bei fehlender Mitgliedschaft), Trainer-Seite um Beitrittsanfragen+Mitgliederliste erweitert, Admin-Nutzerliste um Sperren/Entsperren/Löschen-Buttons. Migration `AddClubMembership` angewendet. Backend-Tests grün, Typecheck/Lint/Build grün, kompletter Flow live per curl verifiziert (Beitritt → Freigabe → Beförderung, Sperren→Login-Block→Entsperren→Login-OK→Löschen→Login-Fail, Passwort-Reset bis zum geloggten Token). Nebenbei aufgefallen, nicht behoben: Demo-Daten enthalten zwei Vereine mit identischem Namen "Hundesportverein Musterstadt e.V." (unterschiedliche IDs) - in der neuen `/clubs`-Browse-Liste dadurch verwirrend doppelt sichtbar; vermutlich Demo-Seeder-Altlast, kein durch diese Änderung verursachter Bug.

- [x] Demodaten bereinigt und an die neuen Komfort-Features angepasst: lokale Dev-DB komplett neu aufgesetzt (der zuvor entdeckte doppelte "Hundesportverein Musterstadt e.V." war ein Datenrest aus wiederholtem manuellem Testen über viele Sessions hinweg, kein Code-Bug - DemoDataSeeder selbst legt nur einen Verein an). `DemoDataSeeder.cs` erweitert: Max/Lisa bekommen jetzt passend zu ihrer bestehenden Gruppenmitgliedschaft auch eine genehmigte `ClubMembership`-Zeile (vorher inkonsistent: Gruppenmitglied, aber laut neuem Mitgliedschaftsmodell kein Vereinsmitglied), neuer fünfter Demo-Account `interessent@dogity.test` (Tom Neuling) mit einer offenen Pending-Beitrittsanfrage, damit der Trainer-Demo-Account ("Beitrittsanfragen"-Liste) sofort etwas zum Testen zeigt, ohne erst manuell eine Anfrage zu erzeugen. Login-Seite um den fünften Demo-Button ergänzt. Verifiziert: frische DB, Migrationen + Seeder laufen sauber durch, Trainer sieht per curl genau die erwarteten 2 Mitglieder + 1 offene Anfrage.

- [x] Fünf weitere Komfort-Features umgesetzt, statt E-Mail-Versand bewusst **In-App-Benachrichtigungen** (Nutzerwunsch: "mach keine emails sondern am besten app benachrichtigungen"). **(1) Benachrichtigungssystem**: neue `Notification`-Entity (Migration `AddNotifications`), `INotificationService`/`NotificationsController` (`GET /api/notifications`, `GET .../unread-count`, `POST .../{id}/read`, `POST .../read-all`), als Nebeneffekt ausgelöst in `ClubService.DecideJoinRequestAsync` (Beitritt angenommen/abgelehnt), `ClubService.PromoteMemberToTrainerAsync` (zum Trainer befördert) und `TrainingService.SetFeedbackAsync` (Trainer-Feedback erhalten). Frontend: Glocken-Icon (`notification-bell.tsx`, neue `components/ui/popover.tsx` auf Basis von `@base-ui/react/popover`) in Sidebar + mobilem Header, `auth-context.tsx` pollt den Ungelesen-Zähler alle 60s (kein WebSocket, Overkill für die Vereinsgröße). **(2) Verein verlassen**: `ClubService.LeaveClubAsync` + `DELETE /api/clubs/{id}/membership`, Button auf `/clubs` bei aktiver Mitgliedschaft - bewusst nur die `ClubMembership` betroffen, keine automatische Gruppen-/Trainer-Bereinigung. **(3) Profil bearbeiten**: neuer `ProfileController` (`GET/PUT /api/profile`, `PUT .../email` mit Passwort-Bestätigung gegen Account-Übernahme, `PUT .../password`) - Avatar bewusst nur als URL-Feld, kein Datei-Upload (keine Storage-Anbindung vorhanden). **(4) Trainer-Feedback-Erinnerung**: `TrainingService.GetPendingFeedbackAsync` + `GET /api/trainings/pending-feedback`, neue Sektion auf der Trainer-Seite - die im Seeder absichtlich unkommentierte `openFeedbackSession` diente als ideales Live-Testmaterial. **(5) Druckansicht**: neue Route `/dogs/[id]/print` mit Trainingsplan + -historie, `window.print()` statt einer neuen PDF-Bibliothek (Supply-Chain-Vorsicht, siehe `pdftotext`-Konvention) - `print:hidden` auf Sidebar/BottomNav/mobilem Header ergänzt, damit nur der Inhalt gedruckt wird. Backend-Tests grün, Typecheck/Lint/Build grün, alle fünf Flows live per curl verifiziert (Beitritt→Freigabe→Benachrichtigung, Beförderung→Benachrichtigung, Verein verlassen, Trainer-Feedback→Benachrichtigung+Pending-Liste schrumpft korrekt, Profil-Update inkl. abgelehnter E-Mail-Änderung bei falschem Passwort und vollständigem Passwort-Wechsel-Roundtrip).

## Roadmap: Technische Schulden × Features (Audit 2026-07-16)

Ergebnis eines vollständigen Code-Audits (Sicherheit, Performance, Wartbarkeit) auf Stand `64ba3b9`, verzahnt mit den offenen Feature-Wünschen. Reihenfolge ist strikt - jeder Schritt ist einzeln über den Test→Prod-Flow deploybar, es ist immer nur eine Baustelle offen. **Entscheidung Auftraggeber: E-Mail-Versand ganz nach hinten** - bis dahin bleibt der bestehende Workflow (Admin bekommt In-App-Benachrichtigung bei Passwort-Reset-Anfragen und setzt das Passwort in der Nutzerverwaltung neu).

### 1. Sicherheits-Sofortfix: Reset-Tokens raus aus den Prod-Logs

- [x] `LoggingEmailSender` loggt den kompletten Passwort-Reset-Link inkl. gültigem Token - auf der Prod-VPS landet das in den Container-Logs (Retention unbegrenzt bis zum nächsten Recreate). ERLEDIGT: Body wird nur noch in Development geloggt (`IHostEnvironment.IsDevelopment()`-Check), außerhalb nur Empfänger + Betreff. Bewusst OHNE den SMTP-Umbau (kommt erst in Schritt 7). Der alte Log-Bestand mit Tokens rotiert durch das Container-Recreate beim Prod-Deploy.

### 2. Quick-Wins Performance/Resilienz

- [x] `GpsTrackService`-Query mit zwei Collection-Includes (`Points` + `WalkRuns.Points`) erzeugt kartesische Zeilenexplosion im JOIN - ERLEDIGT: `.AsSplitQuery()` ergänzt (dafür `Microsoft.EntityFrameworkCore.Relational` als Paketreferenz in Dogity.Application, provider-neutral).
- [x] Offline-Queue (`offline-queue.ts`): `syncQueuedRequests` bricht bei JEDEM Fehler ab - ERLEDIGT: 4xx (außer 401/408/429) → Item verwerfen + Toast mit Label des verlorenen Eintrags; Netzwerkfehler/5xx/401 → Abbruch mit Retry. Dabei einen ECHTEN vorbestehenden Bug gefunden und behoben: Die Queue-Keys waren zufällige UUIDs, IndexedDB `getAll()` liefert aber in KEY-Reihenfolge - die versprochene FIFO-Abspielreihenfolge ("Training vor Fährte") war daher effektiv zufällig. Jetzt chronologisch sortierbare Keys (Zeitstempel + monotone Sequenz + UUID-Suffix); expliziter FIFO-Test ergänzt, Testlauf 5x in Folge grün (vorher flaky).

### 3. Katalog fachlich korrekt (eine PO-Review-Session, keine Doppelarbeit)

- [x] IGP-1-3-Punktaufteilungen gegen die offizielle PO geprüft - ERLEDIGT: Auftraggeber stellte UTI-REG-IGP-de-2025 als PDF bereit; alle Punktwerte stimmen exakt (siehe "Erledigt: IGP1-3 Punktaufteilung" oben), drei Beschreibungsfehler korrigiert.
- [x] Fehlende Prüfungsstufen eingepflegt - ERLEDIGT: 6 neue Sportarten (FPr, UPr, SPr, GPr, StöPr, IAD) + FCI-IGP FH unter der Sportart Fährte, insgesamt 17 neue Prüfungsordnungen mit Punktwerten/Feldgrößen/Suchzeiten/Startvoraussetzungen/Mindestalter direkt aus der PO (S. 68 Zusatzprüfungen, S. 69-79 IGP-FH, S. 80-82 StöPr, S. 83-84 IAD, S. 10 Zulassungstabelle). Punktsummen per SQL gegen frisch geseedete Dev-DB verifiziert (FPr/UPr/SPr/StöPr/IGP-FH je 100, GPr je 200, IAD 0 = bestanden/nicht bestanden).

### 4. Content-Security-Policy

- [x] Report-Only-Phase ausgerollt (2026-07-16): CSP via `headers()` in `next.config.ts` als `Content-Security-Policy-Report-Only`. Policy: script/style 'self'+'unsafe-inline' (Next-Hydration bzw. Leaflet-style-Attribute), img 'self'+data:+blob:+https: (freie Avatar-URLs + OSM-Kacheln), connect 'self'+beide API-Domains (statisch, da NEXT_PUBLIC_API_URL zur Laufzeit fehlt), worker/manifest 'self', frame-ancestors 'none'. Verstöße gehen an den neuen Route-Handler `/csp-report` (same-origin, loggt einzeilig in die Frontend-Container-Logs - `docker compose logs frontend-prod | grep CSP-Report`), damit auch iPhone-PWA-Verstöße zentral sichtbar sind.
- [ ] Nach der Beobachtungswoche (ab ca. 2026-07-23): Reports sichten, Policy ggf. nachziehen, dann Header-Name von `Content-Security-Policy-Report-Only` auf `Content-Security-Policy` umstellen (scharf schalten). Muss vor Schritt 6 (JWT-Härtung) liegen.

### 5. Paket "Hundeseite schnell + wartbar" (strikt zweistufig, größtes Paket)

- [x] **Stufe A (Backend, abwärtskompatibel):** ERLEDIGT (2026-07-16) - `bool hasGpsTrack` in `TrainingSessionDto` (ein Existenz-Lookup für alle geladenen Sessions statt N Frontend-Requests) + `?from=&to=`-Zeitraumparameter (inklusiv) an `GET /api/trainings`. OHNE Parameter unverändert komplette Historie (Statistik-Dashboard, Druckansicht und Plan-Fortschritt unangetastet). 2 neue TrainingService-Tests (Zeitraumfilter, Flag).
- [x] **Stufe B (Frontend):** ERLEDIGT (2026-07-16) - `dogs/[id]/page.tsx` 686 → 215 Zeilen, zerlegt in `training-form.tsx` / `session-history.tsx` / `co-owners-section.tsx`. `GpsTrackSection` nur noch bei `hasGpsTrack || !readOnly` gemountet (Live auf Test: 6 Juli-Sessions, nur 4 GPS-Sektionen = die mit Fährte). Initial nur letzte 3 Monate (`?from=`), "Ältere Trainings anzeigen" lädt Rest (verschwindet danach); leeres 3-Monats-Fenster fällt auf Voll-Historie zurück. Browser-verifiziert auf test.dogity.net: keine Konsolen-Fehler, 36/36 OSM-Kacheln geladen (CSP-Report-Only blockt nicht), Monatsgruppen + Mitbesitzer korrekt.

### 6. JWT-Härtung Stufe 1

- [ ] `SecurityStamp`-Validierung beim Token-Check (Admin-Sperre wirkt damit sofort statt erst nach Tokenablauf) + `ExpiryMinutes` von 1440 auf 120 senken. Bewusst NACH scharfer CSP und ohne offene Frontend-Baustelle, da kürzere Sessions das Nutzererlebnis ändern und isoliert beobachtbar sein sollen.

### 7. E-Mail-Versand (bewusst ganz hinten, Entscheidung Auftraggeber)

- [ ] `IEmailSender`-Registrierung konfigurationsgetrieben machen (`Email:Mode` = `Logging`/`Smtp` statt Code-Änderung in `DependencyInjection.cs`), Provider-Entscheidung (Resend / Brevo / Hoster-SMTP) einholen, Zugangsdaten via `.env`, Testversand auf Test-Env. Bis dahin trägt der Admin-Benachrichtigungs-Workflow den Passwort-Reset vollständig.

### Zurückgestellt (Nice-to-have, bewusst NICHT auf der Roadmap)

- Cookie-Auth + Refresh-Token-Rotation (JWT-Härtung Stufe 2): bricht `api.ts`, 401-Handling, Offline-Queue und Dev-CORS gleichzeitig - erst vor einer öffentlichen Beta mit Fremdnutzern angehen. CSP + SecurityStamp senken das Risiko bis dahin ausreichend.
- Test-Env von `Development` auf eigenes `Staging`-Environment umstellen (Demo-Seeder an explizites `Seed:Demo`-Flag koppeln, CORS festnageln, Swagger schützen): erst nötig, wenn die Test-Domain öffentlich sichtbar beworben wird.
- PDF-Export: Druckansicht `/dogs/[id]/print` + Browser-Print deckt den Bedarf.
- Exercises-Endpoint-Query-Parameter (siehe Performance-Backlog oben): spart nur wenige KB in der Admin/Trainer-Verwaltung.
